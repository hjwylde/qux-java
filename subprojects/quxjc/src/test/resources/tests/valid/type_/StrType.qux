package valid.type_

type A is str
type B is [str]

A f(A a, B b):
    return a + b[1]

void main([str] args):
    print f('bar', ['foo', 'baz'])
    print f('', ['', ''])
    print f('a', ['b', 'c', 'd'])

